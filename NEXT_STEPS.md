# 进度与下一步（2026-09-25）

> 给下一个会话和用户本人的任务书。上一份是 `MORNING_REPORT.md`（09-24 夜班）。

## 当前进度（截至 2026-09-25 凌晨）

- **付费视频下载完成 31/39**：`D:\boke_media`，命名 `{BV}_P{n}.mp4`，51 个分P 共 16.8GB，每个都过 ffprobe 时长校验（≥90% 元数据时长，排除试看片段假成功）。明细见 `work/download_log.md`
- **8 个下不到（权限锁死）**：剥壳 022~028 连续七期 + 26-01-04 范勇鹏现场问答。全是 2026-01-04~02-20 发布，在账号充电覆盖窗口外（03-07 起发布的全部可下）。B 站只发试看流，技术无解；实测证据：48 分钟视频 API 只给 ~29MB 的 1080p 试看规格。试看残file已删，不污染片库
- **补新视频**：重跑 `work/tools/download_exclusive_all.py`（幂等，自动跳过已有，逐个时长校验）。圆脸发新专属后跑一次即可增量补下
- **流水线系统已上线**：`http://192.168.100.102:8765/library.html`，134 测试绿，生产账本播种 5 个待核对分P（北爱威尔士 P1/P2、嘉宾中国行 P1/P2、圆脸X小外全片）
- **调研04 已入库**（ee8d864）：声纹自动认人方案底稿——pyannote 无原生认人，起步复用 community-1 内置 embedding，人物声纹档案按语言分组

## 下一个开发任务：批量导入 29 个新专属视频 + 自动分析

这是主线下一环（下载这批视频就是为了喂流水线）。开工先拿 1 个试导入验证三件事再放量：

1. **路径与命名适配**：播种的 5 个用 `in/` 路径，新文件在 `D:\boke_media` 且多 P 命名 `{BV}_P{n}.mp4`——导入编排（orchestrator）认不认、多 P 怎么拆账本条目
2. **8 个锁死的 BV 标记**：它们还在 300 清单里（`work/space_videos.json`），导入界面要标"无权限不可导"，防误点
3. **全链耗时摸底**：OCR 实测约 2 分钟/分钟视频，29 个约 40 小时内容，CPU 跑要按天算——需要排队或并行策略，先拿 1 个跑通掐表

顺手两个小修（批量跑前修最稳）：

- **busy 滞留清理**：账本条目崩溃后卡 busy=True 无解套口，29 个排队跑一旦中途崩就死锁。修法：服务启动时扫 busy=True 清掉
- **skip 窗词尾截断**（听感问题）：fullmix._stitch_original 与 align_mix.plan_line 借静音交互可能截掉相邻配音词尾——听小样发现字尾吞字就是它

## 用户要做的三件（打开 library.html 核对台）

1. **F5**：听小样拍板"跳过=保留原声"这个语义认不认（目前是假设）
2. **U1**：试听台①区挑嘉宾预设音色（v2 暂用云健男声草稿）
3. **U2**：嘉宾中国行 P1 四人指认（SPK_03 疑似分身待验证）——可参考调研04 的声纹自动认人方案辅助

另外（不急）：

- 查 B 站充电记录确认起始时间是不是 3 月——8 个锁死的申诉依据（App → 我的 → 充电）
- 可选清理：`git branch -d xcheck-night-20260924-163922`（合并后已无差异）
- 8 个锁死视频哪天权限变了（UP 主开放历史专属），重跑下载脚本即可补进
