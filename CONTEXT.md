# CONTEXT.md — 术语表

> 本文件只放术语定义，不放实现细节。实现决策见 README 的决策日志。

## 内容与来源

- **博主（UP主）**：目标订阅的 B 站创作者。日常视频说中文；采访视频中与嘉宾用英文交流。
- **播主清单（uploader catalog）**：博主在 B 站公开的视频目录（标题/时长/日期/充电专属标记）。片库的浏览与勾选来源。
- **片库（library queue）**：已导入视频的任务队列，按状态推进：待下载 → 待分析 → 分析完待核对 → 已核对待合成 → 小样待听 → 已交付；旁路态：已排除（全中文）、失败。视频文件统一存于媒体盘，导入即登记，流程不再依赖目录拷贝。
- **导入（ingest）**：把一个视频纳入片库的动作。来源优先级：B 站播主清单勾选 > BV 号/链接粘贴 > 订阅自动发现（后续）> 本地文件（兜底）。B 站登录凭据过期时（约 30 天），导入暂停并在界面挂提示要求重新导出。
- **目标视频**：博主的英文采访视频——英文原声 + 画面内中文硬字幕（烧录、不可关闭）。配音流水线的输入。
- **充电专属视频**：需向博主"充电"（付费赞助）解锁观看的视频；下载范围的最高优先级。
- **嘉宾**：目标视频中与博主对话的受访者。
- **分P（part）**：一个视频下的独立分段（P1/P2…）。配音决策的最小单元——同一期不同 P 可以是不同形态（如 P1 英文采访、P2 中文解说），各自独立决定做或跳过。
- **主持人（主播）**：当期对话的主导者，目前恒为博主本人。由用户在核对门指认为准；声纹建议可预填主持人，人工仍可改（见"声纹建议"）。

## 声音与克隆

- **中文参考音**：博主说中文的干净人声片段（取自其公开中文视频，10s~1min），用于克隆其音色。
- **同语言克隆**：参考音与输出同为中文——克隆最优档。博主走这条。
- **跨语言克隆**：参考音为外语（英/日等）、输出为中文——效果略降但保留原说话人音色语气。嘉宾优先走这条。
- **预设音色**：不克隆任何人的固定音色（edge-tts / CosyVoice 内置），作为嘉宾克隆不过关时的兜底。
- **人物库（person library）**：全局的"人"的收藏库，与任何单期视频无关。一个人物 = 备注名 + 主要语言 + 若干参考音 + 备注 + 头像。参考音可从已加载视频的说话人解析收录（"这个说话人是圆脸"→ 一键存入该人物），也可手动上传。参考音带**用途标记**（克隆/声纹），克隆选音默认只列克隆标记的；声纹认人见"声纹档案"。主播与常客嘉宾同理入库；主持人配音时从人物库挂接音色。
- **声纹档案（voiceprint archive）**：人物库内按语言分组的声纹样本集合（每人每语言多条），每条 = 说话人音频片段 + 派生声纹向量 + 来源。核对门确认后**自动归档**，无需手动操作；与该人物已有档案明显不符的条目拦截、由人工决定收不收。跨语言样本各归各组，不混。档案随确认积累自动长大，是声纹建议和阈值校准的底料。
- **声纹建议（voiceid suggestion）**：核对门里对每个说话人的自动认人建议——最像的人物 + 相似度分。分数过阈值且领先第二名足够多才**预填**（说话人→人物、主持人指认），永远只预填、不代提交。两个说话人争同一人物时，落选方显示"竞争"提示（常是分身信号）。阈值未经足够样本校准时（冷启动）只显示分数、不预填任何东西，攒够样本自动升级。
- **音色映射表（voices.yaml）**：每期"说话人 → 音色"的映射配置。

## 流程环节

- **硬字幕 OCR**：从画面提取中文字幕文本 + 时间轴；配音文字稿的唯一来源（不用语音听写）。
- **声纹分人（diarization）**：按嗓音把音轨切成"谁在何时说话"，与语言无关（外语原声直接跑）。
- **归人**：核心自研层——字幕条时间窗 × 说话人时间段求重叠占比，把每句字幕判给说话人。
- **语言判定（langid）**：按说话人检测原声语言。全中文的分 P 判"无需配音"；机器判定（whisper 级）在日/中边界不可靠，最终裁决权在用户（听原声样本）。
- **核对门（review gate）**：合成前的人工环节，按分 P 进行：用户看视频、听说话人原声样本，核对人数、指认主持人、修正分身（合并）、给每人配声音（克隆/预设/跳过）、确认或否决"跳过"建议。声纹建议在此预填人物与主持人（可改可关）。
- **合并（merge）**：把被分人拆成两个的同一人并回一个说话人的修正操作。反方向（该分没分）不手修，用"改人数重跑分人"兜底。
- **时长对齐**：让每句配音塞回原句时间窗（限长 → 变速 ≤1.15x → 借静音）。
- **小样（preview）**：全片合成前的 1~2 分钟混音片段（片头 60s + 对话密集处 60s），用户听过"过"才跑全片。
- **交付物**：独立中文配音音频（m4a），通勤收听用；不产出视频。
